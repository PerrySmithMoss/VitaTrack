query GetCurrentUser {
  getCurrentUser {
    data {
      id
      email
      username
      hasGoals
      gender
      profile {
        avatar
        bio
        id
      }
      session {
        userId
        valid
        userAgent
        createdAt
        updatedAt
      }
    }
  }
}
