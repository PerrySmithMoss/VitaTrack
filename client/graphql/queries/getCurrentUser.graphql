query GetCurrentUser {
  getCurrentUser {
    data {
      id
      email
      username
      profile {
        avatar
        bio
        id
      }
      session {
        userId
        valid
        userAgent
        createdAt
        updatedAt
      }
    }
  }
}